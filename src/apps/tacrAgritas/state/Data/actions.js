import ActionTypes from '../../constants/ActionTypes';
import {commonActions as common} from '@gisatcz/ptr-state';

// ============ creators ===========

const add = common.add(ActionTypes.TACR_AGRITAS_DATA);

// ============ actions ===========

// ============ export ===========

export default {
	add
}