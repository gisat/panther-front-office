define([], function(){
	/**
	 * This is layer, which retrieves the WMS information from the server based on the metadata information. It is also
	 * basically a WMS layer. Only the specifics are retrieved in different way.
	 * @constructor
	 */
	var TemplatedLayer = function() {

	};

	return TemplatedLayer;
});
